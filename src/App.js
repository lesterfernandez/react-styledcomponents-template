import { BrowserRouter } from "react-router-dom";
import StyledApp from "./components/styled/App.styled";

function App() {
  return (
    <BrowserRouter>
      <StyledApp></StyledApp>
    </BrowserRouter>
  );
}

export default App;
